import Statistics from "@/components/modules/Statistics";
import { Metadata } from "next";

export const metadata: Metadata = {
  title: "akaDCI | Thống kê",
};

export default function UserManagementPage() {
  return <Statistics />;
}
